export default class PointAddTestModel {
    constructor(pointObj) {
      this.accountId = pointObj.accountId;
      this.todoId = pointObj.todoId;
      this.likeCount = pointObj.likeCount;
      this.created = pointObj.created;
    }
  }
  